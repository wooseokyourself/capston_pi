#ifndef COMMON
#define COMMON

#include <unistd.h>

#include <cstdlib>
#include <cstdio>
#include <cstring>
#include <cassert>

#include <vector>
#include <string>

#define ASSERT assert

namespace Client {
    char* IP;
    unsigned short PORT;
}

#endif